.DS_Store
node_modules
/build
/.svelte-kit
/package
/.firebase
.env
.env.*
!.env.example
# Non-secret Firebase Functions runtime config (e.g. PRINTFUL_CONFIRM) --
# Firebase's own deploy tooling requires this file to exist.
!functions/.env
/.netlify
